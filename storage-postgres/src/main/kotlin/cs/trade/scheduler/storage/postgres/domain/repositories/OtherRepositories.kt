// Placeholder file — every repository has now been promoted to its own file. Kept
// around so the package keeps a documented home for the next stub that lands here
// (e.g. ArchivalSinkRepository / DLQRepository for Phase 2).
package cs.trade.scheduler.storage.postgres.domain.repositories
